#!/bin/bash

export YETTY_SITE_CONFIG="/webapps/yetty/config.json"

cd '/webapps/yetty'
exec 2>&1
exec chpst -u <%= node[:nginx][:user]%>:<%= node[:nginx][:user] %> \
/opt/chef/embedded/bin/unicorn -E production -c /etc/unicorn/yetty.rb
